#include "main.h"
#include <stdlib.h>
#include <string.h>
/**
 * _strlen - return the length of a string
 * @s: length of the string
 * Return: length of the string s
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
