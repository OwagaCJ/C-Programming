#include "main.h"
#include <stdlib.h>
include <stdio.h>

/**
 * _isalpha - check for alphabetic characters
 * @c -  character to be checked
 * Return: 1
 */

int _isalpha(int c)
{

	char c;

	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z))
		return (1);
	else
		return (0)
}
