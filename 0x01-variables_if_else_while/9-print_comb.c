#include <stdio.h>

/**
 * main - print all single digit combinations
 * use the putchar function
 * Return: 0
 */

int main(void)

{
	int n;

	for (n = 48; n <= 97; n++)
	{
		putchar(n);
	if (n == 57)
	{
		break;
	}

		putchar(',');

		putchar(' ';
	}

		putchar('\n');

return (0);
}
