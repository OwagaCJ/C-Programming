#include <stdio.h>

/**
 * main - print all single digit numbers of base 10
 * use the putchar funtion only
 * Return: 0
 */

int main(void)

{

	int i;

	for (i = 0; i <= 9; i++)
	{
		putchar(i);
	}

	return (0);

}
