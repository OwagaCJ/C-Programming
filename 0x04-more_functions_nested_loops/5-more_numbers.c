#include "main.h"

/**
 * more_numbers - prints 0 to 14 ten times
 * use only _putchar
 */

void more_numbers(void)
{
	int i;
	int j;

	for (i = 0; i < 10; 1++)
	{
		for (j = 0; j <= 14, j++)
		_putchar((c / 10) + '0');
		_putchar((c % 10) + '0');
	}
		_putchar('\n');
}
