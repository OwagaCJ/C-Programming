#include "main.h"

/**
 * _memcpy - copy memory area
 * @dest: the destination
 * @src: the source
 * @n: bytes from the momory
 *
 * Return: dest
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
	{
		dest[p] = src[p];
	}

	return (dest);

}
